sim.results <- compare.samplers(sample.size=1000,
                                dists=list(beta23.dist),
                                samplers=list(metropolis.sample),
                                tuning=c(0.1,1,10),
                                trace=FALSE)
print(subset(sim.results,
  select=c('dist','sampler','tuning','act','evals','cpu','err')))
